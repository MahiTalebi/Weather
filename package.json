{
  "homepage": "https://mahitalebi.github.io/Weather",
  "scripts": {
    "build": "tailwindcss build -o ./dist/output.css",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.0",
    "tailwindcss": "^3.3.5"
  }
}
